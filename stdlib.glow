extern double putchard(double i);
extern int printint(int i);

def char wow1() =
  var a <- ['_', ',', 'o'] in
  # var a <- [1, 2, 3] in
  printint(a[1]);

# def wow(array c) =
#   var a <- [1.0, 2.0] in
#   a[0];

# wow([42.0, 46.0]);
# wow1();
wow1();
